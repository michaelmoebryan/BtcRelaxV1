<?phpnamespace BtcRelax;use \BtcRelax\Utils;use \BtcRelax\Model\Bookmark;use BtcRelax\SecureSession;global $core;    function actionCheckActiveOrders() {        $result = false;        return $result;    }        function renderActiveOrders() {        $result = "<p>User has active order!</p>";        return $result;    }    $status = SecureSession::getActualStatus();    switch ($status)    {        case SecureSession::STATUS_ROOT:                        break;        case SecureSession::STATUS_USER:            $cust = $core->getCustomer();                    /* @var $cust type */                    if (isset($cust))                    {                        $header = new \BtcRelax\LayoutHeader($cust);                        /// TODO:  Need to show store front		                        $status = Validation\BookmarkValidator::validateStatus('Published');                        $search = new Dao\BookmarkSearchCriteria();                    }//// Необходимо отобразить витрину                    //$points = BookmarkFactory::GetActiveBookmarks();            renderActiveOrders();            break;        case SecureSession::STATUS_GUEST:            Utils::redirect('guest');            break;        default:            Utils::redirect('main');            break;                }