<?phpnamespace BtcRelax;use BtcRelax\Dao\BookmarkSearchCriteria;use BtcRelax\SecureSession;use BtcRelax\Utils;use BtcRelax\Validation\BookmarkValidator;global $core;    function actionCheckActiveOrders() {        $result = false;        return $result;    }        function renderActiveOrders() {        $result = "<p>User has active order!</p>";        return $result;    }    $status = $core->getSessionState();    switch ($status)    {        case SecureSession::STATUS_ROOT:                        break;        case SecureSession::STATUS_USER:            $cust = $core->getCustomer();                    /* @var $cust type */                    if (isset($cust))                    {                        $header = new LayoutHeader($cust);                        /// TODO:  Need to show store front		                        $status = BookmarkValidator::validateStatus('Published');                        $search = new BookmarkSearchCriteria();                    }//// Необходимо отобразить витрину                    //$points = BookmarkFactory::GetActiveBookmarks();            renderActiveOrders();            break;        case SecureSession::STATUS_GUEST:            Utils::redirect('guest');            break;        default:            Utils::redirect('main');            break;                }