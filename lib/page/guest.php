<?php   namespace BtcRelax;    use \BtcRelax\Utils;    global $core;            $status = $core->getSessionState();            if ($status == SecureSession::STATUS_GUEST)            {            }            else            {                       Utils::Redirect('main');            }?>